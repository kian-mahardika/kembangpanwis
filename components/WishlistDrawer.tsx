'use client';
import {Heart,ArrowUpRight,X} from 'lucide-react';
import {Product,rupiah} from '@/lib/catalog';
import Dialog from './Dialog';
import ProductImage from './ProductImage';
export default function WishlistDrawer({products,onClose,onOpen,onRemove,message}:{products:Product[];onClose:()=>void;onOpen:(p:Product)=>void;onRemove:(p:Product)=>void;message:string}) {return <Dialog message={message} drawer label="Pilihanmu" onClose={onClose}><div className="wishlist-heading"><span className="eyebrow">DISIMPAN DENGAN HATI</span><h2>Pilihanmu <sup>{products.length}</sup></h2><p>Kembang yang ingin kamu ingat.</p></div>{products.length?<div className="wishlist-list">{products.map(p=><div className="wishlist-item" key={p.id}><button className="wishlist-product" onClick={()=>onOpen(p)}><ProductImage product={p}/><div><small>{p.category}</small><h3>{p.code}</h3><p>{rupiah(p.price)}</p><span>Lihat detail <ArrowUpRight size={14}/></span></div></button><button className="remove-button" aria-label={`Hapus ${p.category} ${p.code}`} onClick={()=>onRemove(p)}><X size={17}/></button></div>)}</div>:<div className="empty-state"><Heart size={30}/><h3>Tidak ada produk pilihan.</h3><p>Simpan kembang yang kamu suka dengan menyentuh ikon hati.</p><button className="button primary" onClick={onClose}>Jelajahi koleksi</button></div>}<p className="local-note">Pilihan tersimpan di browser perangkat ini.</p></Dialog>;}
