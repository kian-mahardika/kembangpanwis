'use client';
import {Heart,ArrowUpRight,MessageCircle} from 'lucide-react';
import {Product,rupiah,GOOGLE_FORM_URL,WHATSAPP_NUMBER,whatsapp} from '@/lib/catalog';
import Dialog from './Dialog';
import ProductImage from './ProductImage';
import ShareButton from './ShareButton';
export default function ProductModal({product:p,saved,onSave,onClose,notify,message}:{product:Product;saved:boolean;onSave:()=>void;onClose:()=>void;notify:(s:string)=>void;message:string}) {return <Dialog message={message} label={`${p.category} ${p.code}`} onClose={onClose}><div className="product-detail"><ProductImage product={p} detail/><div className="detail-info"><span className="eyebrow">{p.category}</span><h2>{p.code}</h2><p className="detail-price">{rupiah(p.price)}</p><div className="detail-copy">{p.description.trim()&&<div className="detail-section"><h3>Tentang Kembang Ini</h3><p>{p.description}</p></div>}{p.customization.trim()&&<div className="detail-section"><h3>Sentuhan Kustomisasi</h3><p>{p.customization}</p></div>}</div><div className="order-actions">{GOOGLE_FORM_URL?<a className="button primary" href={GOOGLE_FORM_URL} target="_blank" rel="noopener noreferrer">Pesan Produk <ArrowUpRight size={18}/></a>:<button className="button primary" onClick={()=>notify('Formulir pemesanan segera tersedia. Simpan produk ini terlebih dahulu.')}>Pesan Produk <ArrowUpRight size={18}/></button>}{WHATSAPP_NUMBER?<a className="button secondary" href={whatsapp(p)} target="_blank" rel="noopener noreferrer"><MessageCircle size={18}/> Tanya WhatsApp</a>:<button className="button secondary" onClick={()=>notify('Kontak WhatsApp segera tersedia.')}><MessageCircle size={18}/> Tanya WhatsApp</button>}{(!GOOGLE_FORM_URL||!WHATSAPP_NUMBER)&&<p className="order-note">{!GOOGLE_FORM_URL&&!WHATSAPP_NUMBER?'Pemesanan & konsultasi segera tersedia.':!GOOGLE_FORM_URL?'Formulir pemesanan segera tersedia.':'Kontak WhatsApp segera tersedia.'}</p>}</div><div className="detail-bottom"><button className="text-button" aria-pressed={saved} onClick={onSave}><Heart size={17} fill={saved?'currentColor':'none'}/>{saved?'Tersimpan':'Simpan'}</button><ShareButton product={p} notify={notify}/></div></div></div></Dialog>;}
